import React, { useState } from "react";

const  OldList= () =>{
     const [list, setList] = useState<string[]>([]);

  return (
    <>
        <h2>Moje listy</h2>

    </>
  )
}

export default OldList